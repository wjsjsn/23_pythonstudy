import objectEx11

# 아무 것도 하지 않아도 objectEx11의 내용이 실행 : import 호출의 의미
# => objectEx11의 print(add(1,4)), print(sub(19, 4))가 호출돼 실행됨
# 그러나 objectEx11에 main이 있으면 실행되지 x
# main __name__ : 실행 파일의 이름
# 실행 주체가 되면 __main__으로 변경됨